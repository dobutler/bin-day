{
  "name": "bin-day",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080",
    "build": "vite build",
    "preview": "vite preview --port 8080",
    "test": "node test/smoke.mjs",
    "test:api": "node --import ./test/loader.mjs test/api-check.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "vite": "^6.3.1"
  }
}
